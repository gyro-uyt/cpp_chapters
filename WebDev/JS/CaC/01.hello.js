console.log("Hello, mom");
/*
Evolution of JavaScript Execution Environments
    In the early days, JavaScript code was executed only inside a web browser because the 
    JavaScript engine (such as Google’s V8 or Mozilla’s SpiderMonkey) was tightly integrated 
    with the browser itself. Developers typically had to create an index.html file, 
    link their JavaScript code, and then open it in the browser to view output in the console,
    execute and debug the code using the browser’s console.
    This approach confined JavaScript primarily to frontend and web-based applications.

Modern JavaScript Environments
    Today, these same engines have been extracted from browsers and made available as 
    standalone runtimes like Node.js, Deno, and others. This advancement means developers 
    can run JavaScript code directly on their machines—without needing a browser. 
    As a result, JavaScript is no longer limited to just the web or frontend development; 
    it has become a versatile, general-purpose language used for backend servers, 
    command-line tools, desktop apps, and even IoT devices.
*/
