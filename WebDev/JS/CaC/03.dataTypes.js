"use strict"; // enables modern JS features, old/unsafe syntax not valid

// alert(34+2) // Works in browser only, not in NodeJs
// ';' is optional is JS at end of line

console.log(3 + 2);

let naam = "Ritik"; // string
let age = 21; // int
let isLoggedIn = true; // bool
let BigInt; // for very big no.s
let ans = null; // standalone value, this is not empty
let state; // this is undefined

// there are pther data types also like symbols, object, etc.

console.log(typeof naam);
console.log(typeof age);
console.log(typeof isLoggedIn);
console.log(typeof ans);    // type of null ia object
console.log(typeof state);

