// Document Object Model (DOM)

// for html/browser page, hence following command will not work here, only on browser
// console.log(Window.document);
// console.log(document); // same as above
// document == Window.document

// HTMLCollection, NodeCollection, Array are all different things

// to select an element by ID present in HTML
// document.getElementById("first-heading");

// Now after selection we can apply operations on it
// document.getElementById("first-heading").innerHTML = "<h1>Saitama</h1>";
