#include<iostream>
using namespace std;
int main(){
    cout<<"Hello World"<<endl;
    return 0;
}
// When dividing a decimal value by an integer the result is a decimal value. As 7.0 is a double value the result will be 3.5, 
// if the value would have been 7(integer value) the result would be 3.

// The correct syntax to declare a long variable is (data type)long (variable name)num = (value)95420076541L;
// Why do we appned L at the end of long value?
// Because the value will always be interpreted as int, if not appended with l or L.