from typing import List


class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        k = len(nums)
        store = dict()

        for i in range(k):
            store.update
        return [-1, -1]
