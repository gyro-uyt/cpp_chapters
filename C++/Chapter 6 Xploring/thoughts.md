# Questions

What are clang, cmake, makefile, builds, .vs, .vscode, LLM ?

C++ automatically converts smaller integer types (char, short) to int when used in arithmetic or assignment

- In C++, char is not some magical text-only type. It’s actually an integral type (like int, short, long) but designed to hold very small numbers.
