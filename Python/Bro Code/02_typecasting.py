# Typecasting is the process of converting a variable from one data type tp another (str(), int(), float(), bool(), etc.)

name = "Jonathan Joestar"
age = 25
gpa = 4.2
is_student = True

# gpa = int(gpa)
age = float(age)
age = str(age)
name = bool(name)  # False for empty string/ value

print(type(age))
print(age)
